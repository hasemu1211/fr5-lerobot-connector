# 현재 결과 데스크

고정된 실행 순서가 아니다. 다음 소비자를 가장 빨리 열거나 중요한 불확실성을 줄이는 결과를 선택한다. 상세 실행과 handoff의 현재 owner는 Orca Run `run_45e15721f588`이며, 이전 통합 evidence는 `run_32171e63e7e8`에서 참조한다.

- [x] 검증된 수집 기준선: combined source의 전체 회귀와 focused 계약을 확인하고 remote main에 안전하게 통합한다. 사용자 dirty checkout과 원본 데이터는 보존한다. MEX는 통합 source에서 파생해 확인한다. 다음 소비자는 수집 UI 운영자다. 초기 통합 cutoff `dea09d8bd1d01768daa5ec3abdac77e0bbfb229f`의 검증·보존 근거는 Orca Task `task_af19eb5f36cb`와 Run handoff에서 확인한다. 이후 발견한 운용 결함은 새 cutoff에서 다시 검증한다.
- [ ] 추가 UI 수집 재개: 검증한 코드로 main의 Orca에 UI를 열고 dataset 이름과 기존 적격 범위의 실험 프리셋을 전달한다. 새 startup/중단의 software evidence와 실제 runtime 관찰을 구분한다. 수집 시작과 개별 물리 실행 gate는 운영자가 확인한다.
- [ ] 전체 아키텍처 비판 검토와 가치 있는 교정: Collection부터 Public Documentation까지 모든 lane의 실제 실행·소비 경로와 SSOT, 책임·의존성 경계, 루트부터 하위 구조, 테스트·스크립트 및 증거·산출물 경로를 검토한다. 기존 리서치·source/tests·최신 primary evidence와 실제 PC/runtime을 비례적으로 삼각 검증한다. 유지할 경계와 교정할 단절·중복을 구분하고, 선택한 개선을 구현·검증해야 완료다. 보기 좋은 구조나 검토 보고서만으로 완료하지 않는다.
- [ ] 기존 evidence 탐색 도구의 제품 소비: LeRobot/Rerun native 경로를 재사용해 Web의 정확한 대상에서 두 카메라·action/state를 같은 시각으로 탐색하고 원래 검토 대상으로 복귀한다. 원본·판정·승인 불변, 표시 시각과 canonical identity의 대응, 실패 격리·로컬 노출·자원과 종료를 확인한다. export 단독 성공이나 최신 upstream 문서만으로 완료하지 않으며, 독립적인 선별·학습 연결을 기다리게 하지 않는다.
- [ ] 수집과 검증 양쪽의 시간 비용 개선: 물리 이동·그리퍼·기록·저장·검증·다음 실행 준비와 소프트웨어 테스트·스크립트 실행을 각각 측정하고 주요 비용을 귀속한다. 검증 범위, 데이터 내구성 및 모든 물리·승인 gate를 보존하는 개선을 선택해 같은 경계의 전후 evidence로 확인한다. 단일 관측을 최적값이나 안정적인 분포로 과장하지 않고, 측정상 가치 없는 변경은 하지 않는다.
- [ ] 다회 수집의 자원 상한과 복구: FR5 행 대기열 뒤 실제 이미지 저장·인코딩 경로까지 적체와 자원을 확인하고, 느린 저장에서도 무제한 메모리 증가나 숨은 프레임 유실 없이 처리한다. 장시간 승인 대기, 연속 저장, 일시적인 주행 전 입력 실패, 중단·재시작을 구분하고 완료 데이터 보존·중복 실행 방지·남은 작업 연결을 검증한다. 화면 연결 실패와 실제 실행 실패를 구별해 표시한다. 기존 적격 실물 범위에서 처리량·품질·자원 추이를 확인해야 완료이며, 진단 지표 추가나 짧은 PNG 벤치마크만으로 안정화를 주장하지 않는다.
- [ ] 목적별 충분한 생산 품질 프리셋: 구간별 동작과 촬영·기록 품질을 운용 목적에 맞게 선택하고 기존 정본에서 실제 적용값을 일관되게 해석한다. 소량 수집에서 시간, 안정적인 작업 성공과 사용 가능한 영상·동기화 품질을 함께 확인하고 적용 설정의 계보를 계획과 episode evidence까지 추적해야 완료다. 설정의 적격 표기와 실제 용도 적합성의 근거를 구분하고, 사용자 피드백으로 확인한 그리퍼 설정을 보존한다. 기존 데이터 구분과 승인 의미는 바꾸지 않으며 최대 속도·최대 화질이나 범용 프레임워크 완성을 수집의 선행 조건으로 삼지 않는다.
- [ ] 첫 학습 후보 준비: 실제 frozen source에서 Curator의 prepare → candidate → human decision 경로를 재현한다. physical binding, semantic 판정과 training authorization은 독립적으로 충족해야 하며 원본 episode를 변경하지 않는다.
- [x] 첫 학습·평가 근거: 현재 PC에서 가능한 설정, 고정 split, 승인 dataset과 checkpoint 계보를 연결해 실제 학습 및 held-out 평가를 수행한다. 추가 수집과 평가 조건은 기존 리서치, 최신 primary evidence와 실제 coverage를 함께 근거로 선택한다. 첫 native checkpoint와 전체 held-out 평가의 실행·원본 보존 근거는 Run `run_45e15721f588`과 `outputs/local-learning/`의 launch receipt·checkpoint·평가 보고서에서 확인한다. 이는 실물 작업 성공이나 데이터 충분성의 증명이 아니다.
- [ ] 다음 학습을 위한 데이터 기반 보강: 현재 승인 데이터의 조건별 coverage와 반복을 근거로 기존 Collection이 소비할 유한한 추가 수집안을 정하고, 실제 수집·선별된 결과를 기존 승인·학습 요청으로 연결한다. frozen 비교 원본과 평가 cohort를 보존하고 실제 소비된 dataset·split을 확인해야 완료다. 추천 파일, 목표 개수 도달 또는 UI 기동만으로 완료하지 않는다.
- [ ] 지속 학습과 실물 기준선의 첫 판정: 이미 적격인 설정으로 사전에 정한 학습 예산을 실행하고 중간·최종 checkpoint의 고정 비교 및 전체 비용으로 다음 행동을 결정한다. 별도로 기존 gate를 충족한 정책의 사전 정의 실물 시험을 마쳐 성공·실패·중단·시간을 기록하고 사용 적합성을 판정해야 완료다. runtime smoke, 내부 손실 개선, 생성기 주행 성공을 학습 정책의 실물 성능으로 대체하지 않는다. 세부 recipe와 시험 수는 해당 owner가 실행 전 근거와 함께 정한다.
- [x] 포트폴리오 proof 연결: 표현 책임자가 모방학습·데이터 엔지니어링 직무에 맞는 연구와 시스템의 실제 가치를 조사·선택하고, canonical evidence에 연결된 설득력 있고 시각적으로 검토된 표현물을 만든다. 작은 실제 초안으로 취향을 조율하고 새 성과·반증을 같은 근거 경로에 흡수한다. 구체적인 사례·매체·구성은 dogfooding으로 선택하며 문서 수를 성과로 세지 않는다. physical rollout owner와 실행 evidence가 없으면 실물 effectiveness는 UNKNOWN으로 남긴다. 전체 scenario 충족 후에만 사람에게 archive 확인을 요청한다.

## Portfolio의 작은 구성 검토

- [x] 실제 시안과 사용자 피드백으로 전체 틀과 가치 선택의 시점을 구분한다. 국립국어원 게재 글쓰기 자료와 직접 렌더한 기술 포트폴리오·설명 사례를 비교한 자가 평가(당시 commit `362464d`; 현재는 표현 책임자의 로컬 편집 기록)를 남겼다. 최근 Learning 결과를 대표 사례로 고른 판단을 철회하고, 전체 개요·시스템 구성·미정렬 세부 후보를 왕복 탐색하는 [정적 로컬 틀](../../../docs/portfolio/README.md)로 범위를 줄였다. 세부 항목은 기능·연구·운용·검증의 후보이며 구현 완료·성과·비중을 선언하지 않는다. 상세 evidence와 추적 경로는 기존 owner 및 표현 책임자의 local-only 탐색 자료에 유지한다. 데스크톱과 390px CSS viewport의 렌더·가로 넘침·로컬 링크를 확인하며, 실제 모바일 기기·키보드 입력·디자인 수용은 미확정으로 남긴다. Source 회귀의 실패와 이 표현물의 검증은 Orca Run `run_45e15721f588`의 `msg_c9b34e4e6841`·`msg_e036a1fea679`에서 구분한다. 이 결과는 전체 포트폴리오 proof 연결이나 가치 선택의 완료가 아니다.

- [x] 세부 구성의 획일화를 피하는 편집 기준을 기존 자가 평가(당시 commit `362464d`; 현재 로컬 편집 기록)에 반영했다. 지금 정리할 중복·용어·탐색 경로와 실제 근거를 본 뒤 정할 대표 사례·비중·매체를 구분하고, 후보 목록의 반복 안내를 줄여 단락별 목적을 드러냈다. 바뀐 목록을 데스크톱에서 직접 렌더하고 본문 앵커 이동, 제목 위계, 가로 넘침 없음과 33개 로컬 참조를 확인했다. 현재 전달물은 34,902 bytes로 이미지·영상 포함 전체 50MB 상한 이내이며 모바일 추가 검토는 제외했다. 후보 목록의 형식을 최종 세부 페이지 템플릿으로 사용하지 않으며, 이 정리는 최종 디자인 수용이나 대표 가치 선정의 완료를 뜻하지 않는다.

- [x] 실제 내용을 채운 제품 설명 시안을 만들고 다종 사례로 편집을 재평가했다. Foxglove·Rerun·Linear·Stretch를 직접 렌더하고 Linear 상세 사양을 열어 확인했다. FR5 저장 관측 18쌍, 시간 정렬 구현 도해, owner의 합성 데이터 Curator 화면, 기존 loader 안의 학습 비교를 각 기능에 배치했다. 질문형 제목과 검토 절차 중심 구성을 고치고 시스템·모듈 제목과 간결한 설명체를 적용했다. 36개 PNG의 원래 RGB 일치, 평가 원본 참조, 생성 화면의 134개 로컬 참조·앵커와 7,778,495-byte 전달물 범위를 확인했다. 실제 렌더에서 도해의 교차선을 교정하고 제품 화면의 가독성을 검토했다. 이 중간 결과는 전체 기능 설명이나 최종 디자인의 완료가 아니며 장기 Goal은 계속 활성 상태이다.

- [x] 수집 시스템을 기존 public FAKE 진입점으로 직접 사용해 조건 작성·실행 계획·3개 작업 완료·새 draft 화면을 확인하고, 수집 설명과 작업별 기록 구간 도해에 반영했다. 실제 효과 0과 임시 fixture 정리를 확인했다. [독립 실행 포트폴리오](../../../docs/portfolio/README.md)는 이미지·글꼴·측정 자료 발췌를 함께 포함하며 `index.html`을 직접 열 수 있다. `file://`에서 관측·seed·지표·episode·제품 화면 전환과 원본 앵커, 글꼴 로드를 확인했다. 편집 메모·후보 목록·생성 도구는 로컬로 옮겼고, 독자용 자료에는 PC의 절대 경로와 계정 정보를 포함하지 않는다. 기존 원본의 SHA와 범위를 보존하며 새 결과 장부를 만들지 않았다. 이 전달 경계의 완료는 전체 포트폴리오의 시각·내용 완성을 대신하지 않는다.

## 증거 기반 자동화의 구현 범위

아래는 고정 실행 순서가 아니라 연결의 완료 기준이다. 기존 소량 수집과 첫 학습·평가는 전체 그래프 구현을 기다리지 않는다. 코드로 구현된 연결, 실제 실데이터 실행, 장기 자율 운용을 각각 구분하며 앞 단계의 성공으로 뒤 단계를 완료 처리하지 않는다.

- [ ] 첫 제품 연결: 실제 frozen evidence에서 복수 소비자의 작업이 독립적으로 준비되고, 적어도 한 결과가 다음 제품 owner에 실제 소비되는 경로를 재현한다. 에이전트가 claim이나 다음 입력을 수동으로 조립한 시연만으로 완료하지 않는다. 재전달·입력 변경·실패·승인 대기 시 중복 효과와 권한 전파가 없음을 검증한다.
- [ ] 적격 범위의 자율 실행: 제품이 필요한 수집을 선택하고 기존 owner를 통해 실행한 결과를 선별·승인된 학습·평가와 후속 행동까지 연결한다. 실제 수집·평가·중단·복구 evidence가 필요하며 추천 출력, synthetic test 또는 개발용 Orca Task 완료만으로 달성했다고 말하지 않는다.
- [ ] 사람 개입의 책임별 축소: 반복 확인별 현재 owner와 대체할 관측·판정·권한을 식별한다. 검증 가능한 전환부터 구현하고 잘못된 승인·안전 중단·사람 개입 빈도와 품질·시간 비용을 측정한다. scene, semantic, physical binding, training authority의 현재 미충족 상태를 자동화 계획으로 대신하지 않는다.

- [x] 제품 소개의 개발 범위를 구체화했다. 실제 MoveIt 요청·실행 연결과 LeRobot 학습기 호출, SmolVLA 학습 입력을 확인해 기반 기술과 프로젝트 구현을 구분했다. 첫 화면에는 수집·기록·학습 기능을, 시스템 상세에는 역할 비교와 원본 구현 참조를 배치했다. 저장 카메라의 이름은 원본 필드에 맞추고, 기존 관측·측정값을 유지했다. 로봇 모델 그림은 직접 렌더했으나 전체 작업을 충분히 설명하지 못해 로컬 연구에만 남겼다. 전체 작업 장면의 필요성은 기존 Run으로 전달했다. 독립 Chrome의 로컬 파일에서 클릭·키보드 탐색을 확인하고, 원본 자료에서 돌아올 때 선택한 관측을 보존하도록 고쳤다. 이 편집 결과로 장기 Goal을 완료하지 않는다.

- [x] 동작 오차 비교를 수치 표에서 읽기 쉬운 그림으로 확장했다. 관절 여섯 축은 같은 rad 눈금으로, 그리퍼는 별도 m 눈금으로 비교하며 seed·MAE/RMSE·episode 선택에 따라 원본 수치와 그림을 함께 바꾼다. 원본 자료 왕복 시 선택을 보존하고 loader 수정 전 결과임을 표시했다. 실제 Chrome에서 전체 비교·개별 episode·빠른 조건 변경과 출처 왕복을 확인했다. Rerun 기본 탐색과 연구 비교 그림의 역할을 구분하고 범용 탐색기를 추가하지 않았다. 이 결과는 장기 포트폴리오 완료나 최신 loader의 재평가 결과를 뜻하지 않는다.

- [x] 실제 수집 사례를 기존 기록·평가 설명과 연결했다. Episode 27의 수집 결과·기술 검사·학습 provenance 참조를 대조하고, 보존된 세 관측의 프레임과 원래 ROS 시각을 확인했다. 같은 기록이 TRAIN에서 제외되고 개발용 평가에 소비된 경로를 보여준다. 수집 페이지의 사진에서 해당 시점의 두 카메라로, 평가 링크에서 같은 episode의 비교 그림으로 이동하는 실제 브라우저 동작을 확인했다. 신규 영상 추출이나 실행 없이 기존 이미지를 재사용했으며, 세 표본을 전체 동작 시연이나 실물 성공률로 확대하지 않는다.

- [x] 전체 독자 경로를 점검해 소개 범위와 강조를 정리했다. 데이터 페이지의 제목·소개에 기록과 영상 검토를 함께 명시하고, 수집·데이터 페이지에서 원하는 구간으로 바로 이동할 수 있게 했다. 아키텍처는 실제 모듈 이름과 데이터 흐름을 중심으로 표시하며 승인된 학습 입력 조건을 유지했다. 다섯 페이지의 1440×1000 렌더, 수정 도해의 글자·화살표·배경, 독립 Chrome의 Tab·Enter 구간 이동을 확인했다. 이 점검은 전체 실물 시연이나 이후 제품 연결의 검증을 대신하지 않는다.

- [x] 실행기와 기록기의 분리를 실제 운용 이벤트로 설명했다. 동일한 Episode 27의 원래 상태·phase 이벤트에서 집기 종료, 기록 종료, 재배치·복귀와 저장 확정의 순서를 확인해 시간축으로 표현했다. 원본 run·plan 결속과 단조 시각을 대조하고 기존 episode 원본 화면에 발췌를 추가했다. 기록 종료 상태를 저장 함수 처리 시간이나 수집 처리량으로 해석하지 않으며, 실제 렌더로 두 상태 행과 종료 지점의 가독성을 확인했다. 새 로봇 실행이나 데이터 변경은 없다.

## 자율 lane의 작은 결과와 공동 검증

각 lane 책임자가 연구·실제 사용·source/tests·환경 근거에서 다음 작은 Goal을 선택한다. 아래 결과를 미리 지정한 구현 목록이나 직렬 순서로 해석하지 않는다. Goal의 완료는 해당 결과만 증명하며 전체 제품의 완료가 아니다. 공유 계약 초안은 coordinator가 통합하고 상세 실행·자원·후속 assignment는 Orca가 관리한다.

- [ ] 자율 lane 운용의 첫 검증: 책임자가 발전 가설을 선택하고 완료 조건을 세워 실제 소비자가 있는 개선을 구현·검증한다. 공동 경계 충돌, 사용자 개입과 통합 재작업을 기존 Orca evidence로 확인해 운용을 조정한다. 독립 Goal 또는 세션의 개수만 늘린 것은 성과가 아니다.
- [ ] 연구에 근거한 최소 데이터 가치 실험: 기존 연구와 최신 primary evidence·source/tests·현재 PC/FR5 조건으로 반증 가능한 질문을 선택한다. 현재 후보는 동일 추가 수집 예산의 정책 관측 기반 표적 수집 대 기존 균형 수집이다. 성공·실패 evidence에서 runtime 교정과 데이터 요구를 구분하고 선택한 가설을 실제 선별·승인된 학습·재평가로 검증한다. 선택용 development evidence와 최종 평가 cohort를 분리하고, 수집·학습·reset·중단·사람 개입 비용 및 불확실성을 보고한다. 비교 가능한 긍정 또는 부정 결과가 기존 canonical evidence에서 공개 claim까지 추적돼야 완료이며 연결 성공이나 단일 성공 주행으로 대신하지 않는다.

## 증거와 환경 정리의 시점

검증된 결과가 생길 때 기존 public docs의 claim → output → evidence → limitation → next consumer 연결을 갱신한다. raw evidence는 원래 owner에 두며 문서가 현재 상태의 복제 장부가 되지 않게 한다.

전체 구조의 비판 검토를 현재 Goal에 포함한다. 검토 범위를 당장 고치는 파일로 축소하지 않되, 구현 범위는 실제 수집·학습 연결, 반복 실패, 검증 비용과 portfolio evidence에 미치는 가치로 선택한다. 루트부터 하위 깊이까지 실제 탐색 혼선·중복·반복 수정 비용을 확인하며, 파일 크기나 폴더 깊이만으로 재편하지 않는다. 전체 재편을 수집·학습의 선행 조건으로 삼지 않고, 독립적으로 안전한 lane은 병렬로 계속한다.

## 포트폴리오의 장기 완료 범위

2026-09-06 직접 요청으로 확인: 위 작은 구성·편집 결과는 중간 산출물이다. 표현 책임자는 기존 세션의 장기 native Goal 아래에서 실제 내용, 임시 편집 방향, 주제별 매체와 시각 언어를 자율적으로 제작·렌더·사용·수정한다. 대표 가치의 조기 확정을 피한다는 원칙을 모든 내용·디자인 개발의 영구 보류로 해석하지 않는다. 실제 근거와 한계를 갖춘 독자 경험의 설득력·탐색·가독성·시각적 마감을 검토하고 중요한 결함을 해결해야 전체 포트폴리오 결과를 완료할 수 있다. 작은 시안은 여기서 확인하되 매 시안마다 승인이나 새 Goal을 요구하지 않는다. 데스크톱/전달물 총50MB와 기존 원본·환경·공개 권한을 유지한다.

- [x] Curator 설명의 주자료를 실제 FR5 영상 미리보기로 보완했다. 사용자가 지정한 main `outputs/curator/setup/`의 원본·보존 영역·처리 결과와 31개 표본 영상을 소비하고, 기존 합성 UI 화면은 별도로 펼쳐 보는 예시로 옮겼다. 원본 파일 SHA와 픽셀 보존·배경 치환의 일치를 확인했으며 독자 화면에서 영상 재생·시점 이동을 직접 확인했다. 이후 수정 작업도 [기존 Curator 출처](../../../docs/portfolio/sources/curator-screen.html#real-preview)에 보존한 preview·profile 식별자를 따라 실제 자료를 사용한다. 이 자료는 사용자가 확인한 시각적 미리보기이며 학습 데이터 발행·실물 결속·학습 성능을 증명하지 않는다.

- [x] 통합된 Rerun 탐색을 실제 Episode 29 화면과 owner의 열기·복귀 기록으로 설명했다. frame 470과 원래 global index 25548, 같은 대상·검토 내용의 유지와 읽기 전용 상태를 대조했다. 원래 화면 이미지를 포함하고 기존 저장 관측 출처에 실행 발췌를 연결했다. 범용 viewer나 대용량 RRD를 전달물 안에 추가하지 않았다. main의 `msg_4485cd788b21`은 전체 reader의 기반 기술·직접 구현 구분, 판정·권한 경계와 역사적 학습 결과 설명에서 차단할 사실 오류가 없음을 확인했다. 전체 개요→기능별 설명·실제 사례→원본·전체 복귀의 독자 경로와 시각적 마감을 검토했고, 중요한 결함으로 발견한 합성 화면 중심 설명과 실제 연결의 공백을 보완했다. 이 표현 결과의 완료는 다른 lane의 미완료 연구·실물 실행이나 OpenSpec 전체 archive를 뜻하지 않는다.

- [x] 2026-09-07의 직접 피드백으로 독자용 명칭과 도메인 설명을 개정했다. 본문은 나무 큐브 집기·실행과 저장·동작 생성 등 작업과 원리로 제목을 붙이고, 내부 episode/frame 번호는 원본 식별과 연결에 보존했다. 시연 비교의 표시값은 원래 프레임 수와 FPS로 계산한 저장 영상 길이이며 임의의 사례 번호를 추가하지 않았다. 수집의 층화 표본화·물체 footprint 경계 축소, 신호별 시간 정렬, 마스크·중앙값 배경 합성, 실행·저장 분리와 동시성 제어, SmolVLA의 조건부 동작 생성·flow matching·시연 단위 분할·정규화·오차 지표를 실제 구현과 공식 문헌에 맞춰 설명했다. 실제 렌더에서 반복되는 아키텍처 설명을 줄였고, 여섯 시연 선택과 관측 원본 대응, 개별 평가 선택을 브라우저에서 확인했다. 기존 연구 수치·관측 원본과 학습/실물 경계는 유지한다.

- [x] 2026-09-07의 후속 피드백으로 모듈 간 입력·실행 조건·출력을 선택해 읽는 시스템 연결 설명을 만들었다. main MEX를 먼저 읽고 main 코드와 대조해 Rerun의 동기 탐색과 FR5의 대상·원본 대응·복귀·학습 입력 책임을 구분했다. MEX에서 빠진 Rerun 책임과 동작 판정/저장 후 검토의 순서 구분은 root에 전달했고, main의 보완을 확인했다. 실제 Curator 검토 영상에서 사람이 등장한 동일 시점의 세 패널을 원본 헤더와 함께 보존해 표시했다. Foxglove·Distill·Stripe를 직접 렌더해 비교하고, 구성도의 연결선 줄바꿈과 근거에서 돌아오는 선택 복원을 고쳤다. 별도 폴더에 압축을 풀어 일반 Chrome에서 네 연결 선택·근거 왕복, 영상 재생·시점 이동, 기존 평가 선택과 다섯 본문 페이지를 확인했다. 전달물에는 시작 안내와 로컬 매체를 포함하며 Rerun 실행 서버나 원본 학습 데이터셋은 포함하지 않는다.

- [x] 최종 전달 형식을 압축 해제가 필요 없는 단일 HTML로 바꿨다. 편집 원본은 유지하고 Python 표준 라이브러리 생성 명령으로 같은 배포 파일을 다시 만든다. 이미지·영상·글꼴·28개 본문 및 근거 페이지를 포함한 결과는 약 23.4MB이다. 파일 하나만 별도 임시 폴더에 복사한 뒤 네트워크를 끈 Chrome에서 모듈 연결 4개와 근거 왕복, 수집 화면 3개, 저장 관측 18개, 영상 재생·시점 이동, 평가 그림 28개와 선택 복원, 브라우저 뒤로·앞으로 이동을 확인했다. 실제 화면도 검토했으며 이 결과는 배포 방식의 완료이다. 내용과 디자인을 배포본에서 중복 편집하지 않는다. 완료된 임시 프로필·중복 해제본 약 106MB를 정리하고 이후 검증용 폴더는 종료 시 자동 정리한다. 원본과 이전 최종 전달물·검토 근거는 보존했다.

- [x] 아키텍처의 기능 나열과 시각적 정렬 결함을 직접 피드백으로 교정했다. main MEX와 `bd18438` 구현에서 확인한 Collection Operator·Curator·Training Review·Policy Learning을 상위에 두고 내부 모듈과 전달 데이터, 외부 기반 기술을 구분했다. Diffusion Policy의 모델 비교와 π₀의 구성요소 계층을 직접 렌더해 비교했다. draw.io 텍스트 기본 스타일이 중앙 정렬을 덮어쓰던 원인을 고치고, 좁은 실행기 도형의 줄 간격과 체크포인트 표현을 조정했다. 생성된 단일 HTML에서 실제 화면을 다시 확인하고 기존 오프라인 탐색·영상·관측·평가·근거 왕복 검증을 유지했다. 전달물은 약 24.1MB이며 코드의 책임·권한·원본 수치를 변경하지 않았다. 이 편집 결과는 코드 패키지 개편이나 미검증 consumer의 구현을 뜻하지 않는다.

- [x] 사용자가 선호한 모듈·도메인 이름을 다섯 독자 페이지의 제목·메뉴·관련 링크와 하위 모듈 설명에 일관되게 적용했다. 기존 모듈 구성도는 보존하고, 실제 수집 원본이 다음 수집·Candidate Review·Video Transform으로 분기하는 경로와 Training Review/NativeInspection의 탐색·복귀를 별도로 펼쳐 보도록 추가했다. main MEX와 고정된 `881cfdd` 소스, OpenSpec의 미완료 자동화 결과를 대조해 현재 분기 구조·향후 제품 자동화·개발용 Orca DAG를 구분했다. 새 소스 발췌는 기존 OneJob 근거 페이지에 연결하고 같은 분기 설명으로 돌아오는 동작을 구현했다. 실제 렌더로 전체 페이지의 제목·메뉴와 도해를 확인하고 오프라인 단일 HTML에서 펼치기·접기·근거 왕복 및 기존 상호작용을 검증했다.
