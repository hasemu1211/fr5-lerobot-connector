schema: spec-driven
context: |
  Shared product requirements live in
  openspec/changes/establish-portfolio-proof-loop-intent/specs/portfolio-proof-loop/spec.md.
  Every lane must apply its requirements, especially "Curator owns reusable acquisition
  recommendations", "Native owners enforce safety without duplicate operator gates",
  and "Existing product interfaces precede custom capabilities".
  Lane changes reference these shared requirements instead of copying a second rulebook.
  Specification acceptance is not evidence that implementation or physical execution is complete.
