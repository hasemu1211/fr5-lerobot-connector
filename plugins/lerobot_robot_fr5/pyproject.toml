[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "lerobot_robot_fr5"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["lerobot==0.6.1"]

[tool.setuptools.packages.find]
where = ["src"]
